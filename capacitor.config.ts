import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.noorfuel.dispenser',
  appName: 'Noor Fuel Automation',
  webDir: 'dist'
};

export default config;
